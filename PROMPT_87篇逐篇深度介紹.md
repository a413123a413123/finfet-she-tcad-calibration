# PROMPT：FinFET SHE 文獻目錄 — 87 篇逐篇深度介紹

> 使用方式：開一個**新的** Claude Code session，工作目錄設在 `C:\Users\阿光\Desktop\黃育德`，
> 把下面 `===== PROMPT 開始 =====` 到 `===== PROMPT 結束 =====` 之間**整段**複製貼上。
> （本檔最後附「這份 prompt 為什麼這樣寫」，那部分不要貼。）

---

===== PROMPT 開始 =====

## 0. 角色與情境

你是半導體元件領域的研究助理。使用者是 AI 碩士生（具電機背景），正在做**矽 FinFET 自熱效應（SHE）對飽和區汲極電流影響**的 TCAD 模擬研究，需要這份逐篇介紹達成兩個目的：

- **(A) TCAD 模擬校準依據**：從文獻取得 Rth / ΔT / 偏壓 / thermode 邊界條件的合理量級，用來校準與 sanity-check 自己的 deck。
- **(B) 快速建立領域全貌**：知道哪幾篇是必讀核心、哪些是離群值、哪些不能引用。

回覆語言：**繁體中文**。技術術語、論文標題、逐字引用一律保留英文原文，不翻譯。

## 1. 輸入檔案

- 主檔：`C:\Users\阿光\Desktop\黃育德\FinFET_SHE_saturation_Ion_121papers (3).pdf`
- 已抽好的純文字：`C:\Users\阿光\Desktop\黃育德\_121papers_fulltext.txt`

**已知的坑（先讀，別重踩）：**
- 這份 PDF 的中文用一般方式讀會亂碼；若純文字檔不存在，用 PyMuPDF 重抽，並確保以 UTF-8 讀寫。
- **論文標題在 PDF 內被截斷**（結尾有 `…`）。**DOI 是唯一可靠的識別鍵**，一切以 DOI 為準。

**文件本體**是一份已做過對抗式查證的 121 篇文獻總表，結構如下：

| 章 | 主題 | 篇數 | 本次處理 |
|---|---|---|---|
| 一 | 直接結論（敘述段，非表格） | — | 用作總覽頁素材 |
| 二 | 電熱 vs 等溫 — 矽 FinFET | 16 | ✅ |
| 三 | 電熱 vs 等溫 — 相鄰 nanosheet / GAA | 14 | ✅ |
| 四 | 量測（pulsed-vs-DC / TRE） | 8 | ✅ |
| 五 | 熱阻 / 溫升導向（Rth、ΔT） | 45 | ✅ |
| 六 | 可靠度 / 其他指標（HCI、BTI、TDDB、gm/gd、電路級） | 34 | ❌ **完全排除，一篇都不要碰** |
| 七 | 環境溫度掃描（ta_sweep，注意這不是 SHE） | 4 | ✅ |

表格欄位為：`論文/IEEE或非/年/DOI ｜ 元件 ｜ 關鍵數字 ｜ 偏壓或ΔT/其他 ｜ 查證狀態`。
查證狀態的既有標記有：`confirmed`、`plausible_unverified`、`value_corrected`、`reclassified`。

## 2. 任務

對**第二、三、四、五、七章，共 87 篇**，逐篇產出一張「半頁深度卡」。

**開工第一步（不可跳過）**：從 `_121papers_fulltext.txt` 用正則抽出這五章所有 `DOI:` 條目，建立 87 筆清單寫成 `WORKLIST.md`（欄位：序號｜章別｜DOI｜PDF 內截斷標題｜原表查證狀態）。
- 若抽出的數量不是 **16 / 14 / 8 / 45 / 4（合計 87）**，**停下來先解決解析問題再繼續**，不要硬做。
- 之後以 `WORKLIST.md` 為唯一權威清單逐筆消化。

**規模紀律**：87 篇一篇都不能少、不能合併、不能抽樣、不能寫「其餘 N 篇類似」。

## 3. 每篇都必須上網查證（硬性要求）

每篇至少走完這條取得鏈，直到拿到內容為止：

1. `https://doi.org/<DOI>` 解析到出版者頁
2. 出版者頁擋住 → Semantic Scholar API：
   `https://api.semanticscholar.org/graph/v1/paper/DOI:<doi>?fields=title,abstract,year,authors,venue,externalIds,openAccessPdf`
3. → OpenAlex：`https://api.openalex.org/works/doi:<doi>`
4. → arXiv / PMC / 機構 repository / 作者個人頁 / Google 可索引之全文
5. 全部失敗 → 記為 `取得層級：未取得（付費牆）`

### 反幻覺紀律（違反即視為任務失敗）

- 只能寫**你這次實際讀到的文字**。**嚴禁從標題、期刊名、年份或訓練記憶推測論文內容。**
- **每一個數字後面都必須標** `[來源: <URL>｜層級: 全文 / 摘要 / 僅 metadata]`。
- 讀不到就大方寫「未取得」——這是可接受的答案；編造不是。
- 原表已標 `confirmed` 的逐字數字視為既有事實：若你查到不同值，**兩個都列並標 `⚠CONFLICT`**，不得逕自覆蓋。
- 若發現原表有錯（年份、分類、DOI、數值），標 `⚠CORRECTION` 並附證據 URL。
- **不得新增原表以外的論文**，也不得自行擴充搜尋主題。

## 4. 每張卡片的固定八欄（缺一不可；無資料就寫「未取得」）

1. **識別** — 完整標題（補全被 `…` 截斷的部分）／第一作者 + 單位／期刊或會議全名／年／DOI／IEEE or 非 IEEE
2. **元件** — 技術節點、bulk / SOI / GAA / nanosheet / CFET、Lg、fin 或 sheet 幾何（Hfin、Wfin、TNS、WNS、堆疊數）、n or p、鰭數／指數
3. **方法與 SHE 定義** — 歸類為下列哪一種，並說明工具與設定：
   - `ET-vs-ISO`（同一 deck 開／關晶格加熱之差 ← 模擬中 SHE 的正確定義）
   - `pulsed-vs-DC / TRE`（實測定義）
   - `Rth / ΔT 萃取`
   - `ta_sweep`（改環境溫度，**這不是 SHE**）
   - `解析 / 緊湊模型`

   若為 TCAD：求解器（Sentaurus / GARAND / 其他）、熱邊界條件、**thermode 位置與 SurfaceResistance 設定**、有無 hydrodynamic / BTE / 聲子模型。
4. **關鍵定量結果** — Ion 下降 %、Rth、ΔT、峰值晶格溫度，各自標明對應偏壓（VGS、VDS、TA）。
   逐字引用原文英文句，並明確標示是「**原文印出數字**」還是「**僅圖層級**（需自讀原圖才能引用）」。
5. **對 TCAD 校準的用途（用途 A）** — 這篇能餵給我的 deck 什麼？可用的 thermode SurfaceResistance 量級、可比對的 ΔT、可對齊的幾何、可拿來 sanity-check 的判準。若不可用，明寫「不可直接校準，理由：⋯」。
6. **批判（至少 3 點）** — 熱邊界是否過絕熱或過導熱？單一幾何能否外推？數值是否含糾纏因子（應力、interface traps、DMG 通道工程等非純 SHE 貢獻）？分類是否有誤（最常見陷阱：ta_sweep 被誤當 SHE）？有無誤差棒與重複性？**並判定其數字落在下節判準帶內或外，外的話原因是什麼。**
7. **可引用性等級** — `A 可直接引用數字` / `B 只能引用定性結論` / `C 僅可當背景引用` / `D 不建議引用`，附一句理由。
8. **取得狀態** — 本次實際讀到的層級 + 來源 URL 清單 + 與原表的差異標記（無差異寫「與原表一致」）。

## 5. 判準帶（逐字沿用本目錄第一章，不得改動；寫進總覽頁，並用於第 6 欄判定）

- 標準矽 FinFET、VDD~0.7 V、TA=300 K、電熱 vs 等溫：飽和 Ion 下降 **~7–11%**（判準帶 bulk 3–12%、SOI 8–17%）
- 交叉檢核比值：**ΔIon% ÷ ΔT ≈ 0.10–0.20 %/K**
- 飽和 Ion 差 **>20%**（排除 DMG / junctionless 特例）→ SHE 過強 → 查 thermode 是否過絕熱
- 飽和 Ion 差 **<1%** → SHE 被邊界條件抹掉 → 查 thermode 是否貼太近通道
- **single-fin Rth ≈ 1–4 MK/W**；多鰭多指 RF 結構 ≈ **34 kK/W**
- 核心逐字錨點：14nm FinFET n 7.26% / p 8.91% @VDS=0.7V（10.1088/1402-4896/adb2f1）；3nm bulk FinFET n 10.6% / p 21.6% @VG=VD=0.7V（10.1038/s41598-026-46949-1）；bulk FinFET 7.8%（Myeong, TED 2019, STI）

## 6. 分批與進度控制（必須做）

分 7 批，**每批做完立刻落地檔案**，不要等全部做完才寫：

| 批 | 內容 | 輸出檔 |
|---|---|---|
| 1 | 第二章 16 篇 | `cards/02_ET_vs_ISO_FinFET.md` |
| 2 | 第三章 14 篇 | `cards/03_ET_vs_ISO_NS_GAA.md` |
| 3 | 第四章 8 篇 | `cards/04_measurement.md` |
| 4 | 第七章 4 篇 | `cards/07_ta_sweep.md` |
| 5 | 第五章 第 1–15 篇 | `cards/05_Rth_dT_a.md` |
| 6 | 第五章 第 16–30 篇 | `cards/05_Rth_dT_b.md` |
| 7 | 第五章 第 31–45 篇 | `cards/05_Rth_dT_c.md` |

每批結束更新 `PROGRESS.md`，內容包含：已完成 DOI 清單、累計 n/87、未取得全文的 DOI 清單、`⚠CONFLICT` 清單、`⚠CORRECTION` 清單。
**中途若被中斷，下次從 `PROGRESS.md` 續作，不要從頭重做。**

可以用平行 subagent 加速（建議每個 subagent 負責 8–10 篇，只回傳結構化卡片文字），但**主 session 必須逐筆核對 DOI 清單完整性**，不得相信 subagent 自報的篇數。

## 7. 最終輸出：網頁 artifact

87 張卡全部完成後，做一個單頁 HTML 並發布為 artifact：

- **頂部總覽**：87 篇分佈（依章節 / 年份 / IEEE vs 非）、判準帶說明、核心錨點數字、**「必讀 Top 10」並逐篇說明為何必讀**（以 TCAD 校準價值 + 可引用性排序）
- **卡片區**：87 張卡，預設收合只顯示第 1、4、7 欄，點開展全部八欄
- **篩選器**：章節 / 可引用性 A–D / 有無印出數值 / 方法類型（ET-vs-ISO、pulsed、Rth、ta_sweep、模型）/ IEEE vs 非 / 年份區間 / 關鍵字搜尋（標題 + 元件 + 數字）
- **排序**：年份、可引用性、Ion% 大小
- 明 / 暗主題皆可讀；表格與長內容各自橫向捲動；**不引用任何外部資源**
- **先寫成本地檔** `C:\Users\阿光\Desktop\黃育德\87papers_review.html` 再發布，方便日後改版重發同一 URL

## 8. 完成定義（DoD）— 全部達成才可回報完成

1. `WORKLIST.md` 有 87 筆 DOI，且與 PDF 五章逐一比對無缺漏
2. `cards/` 下七個檔案的卡片總數 = 87
3. 每張卡八欄齊全；**每個數字都有來源標註**；沒有任何一句是推測
4. `PROGRESS.md` 顯示 87/87，並列出未取得全文的篇目
5. artifact 已發布並回報 URL

若有任何一項未達成，**必須明講哪幾篇沒做完、為什麼**，不得含糊回報「已完成」。

## 9. 明確禁止

- 碰第六章（可靠度 / HCI / BTI / TDDB / gm-gd / 電路級，34 篇）
- 合併相似論文、寫「其餘 N 篇類似」
- 用訓練記憶補摘要或補數字
- 為了縮短輸出而刪欄位、減篇數
- 自行擴充搜尋範圍、新增目錄外的論文
- 把 `ta_sweep` 的 % 當成 SHE 劣化數字引用

===== PROMPT 結束 =====

---

## 附註：這份 prompt 為什麼這樣寫（給你自己看的，不用貼）

寫長任務 prompt 的七個要點，這份都用上了：

1. **給輸入的確切路徑＋已知的坑**
   不只給檔名，還先講清楚「中文會亂碼」「標題被截斷、DOI 才是 key」。你踩過的坑寫進 prompt，對方就不用重踩一次。

2. **把範圍寫成可驗證的算式**
   不寫「除了第六章」，而寫「16+14+8+45+4 = 87」，並要求對方先自建 WORKLIST 對數。這讓「有沒有做完」變成可稽核的事實，而不是模型的自我感覺。

3. **把「不知道」定義成合法答案**
   最重要的一條。如果 prompt 只說「逐篇介紹」，遇到付費牆的篇目模型幾乎必然編造。明講「未取得是可接受的答案，編造不是」，幻覺率會斷崖式下降。

4. **每個數字強制附來源與取得層級**
   `[來源: URL｜層級: 全文/摘要/僅metadata]` 這個格式讓幻覺無處可藏——編出來的數字寫不出來源。

5. **固定欄位模板**
   八欄固定，一方面讓 87 篇可橫向比較，另一方面防止模型在後半段偷懶縮水（少寫一欄一眼就看得出來）。

6. **分批 + 進度檔 + 可續作**
   87 篇 × 上網 × 半頁深度是很重的任務，一定會中斷。每批落地 + PROGRESS.md 讓它能續作，也讓它無法假裝完成。

7. **寫死 DoD 與禁止清單**
   「完成定義」把驗收標準交給對方自查；「明確禁止」則是把你已知的失敗模式（合併、抽樣、用記憶補、混淆 ta_sweep）逐條堵死。

### 兩個實務提醒

- **這個任務量很大**：87 篇每篇都要 DOI 解析＋多來源 fallback，加上半頁深度撰寫，可能跑不完一個 session。分批機制是必要的，不是可選的。
- **付費牆命中率**：原表 89 篇 IEEE，其中很多是 TED/IEDM/IRPS 全文付費。實際上會有相當比例停在「摘要層級」。這是資料現實，不是執行失敗——prompt 已經允許誠實回報，你驗收時看 `PROGRESS.md` 的「未取得清單」長度就知道實際覆蓋率。
